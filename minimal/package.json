{
  "name": "portfolio-minimal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Portfolio — variante document : deux pages statiques, zéro dépendance, zéro JavaScript.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node serve.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "fonts": "node scripts/fetch-fonts.mjs",
    "check": "npm test && npm run build"
  }
}
